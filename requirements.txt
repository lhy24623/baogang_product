# ============================================================
# Python依赖配置文件
# ============================================================
# 使用 pip install -r requirements.txt 安装所有依赖

# Flask: 轻量级Web框架，用于创建后端API服务
flask>=2.0.0

# pymysql: MySQL协议客户端，Doris兼容MySQL协议，用于SQL增删改查
pymysql>=1.0.0
